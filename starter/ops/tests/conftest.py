"""pytest configuration for ops contract tests.

The tests run from the `starter/ops/tests/` directory and need no
special path setup. Kept as a placeholder so that future ops
fixtures (load-test profiles, sample Kustomize overlays) have a
home.
"""
